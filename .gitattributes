# Normaliza los finales de línea a LF en el repositorio para evitar
# diferencias entre Windows y los runners Linux/macOS de CI.
* text=auto eol=lf

# Binarios: sin normalización.
*.png binary
*.ico binary
*.jks binary
*.keystore binary
